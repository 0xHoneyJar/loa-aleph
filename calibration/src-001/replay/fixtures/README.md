These newly authored artificial bytes are mechanics scenarios, not semantic
ground truth. No SRC-001 answer content was used.

H-F1 uses the source/criteria and an opaque synthetic PDF marker, synthetic
forbidden canaries, release receipts, and captured-context records.
H-F2 uses complete and blocked artificial filesystem snapshots.
H-F3 supplies independently named dummy records and exact raw-byte selectors.
H-F4 carries an unresolved mapping, an unreached stage, and an uncertainty
bucket. Their identities start with `SYNTHETIC-`; none is a replay attempt.

The test factory writes only under a new `SYNTHETIC-` temporary root. It does
not read the retained reference archive or create real replay storage.
Synthetic A/B receipts name the required checkout pins to test the protocol;
they are not records of a canonical release build.
