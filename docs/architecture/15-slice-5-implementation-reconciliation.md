# Slice 5 Internal Ambiguity Lifecycle — Implementation Reconciliation

> Date: 2026-09-04
>
> Authorized implementation base:
> `7944964393aab98416772f6a37be967d9ffdece2`
>
> Authorized base tree:
> `1feab15bbc1c3eadab4aaa948a6803967b44d47a`
>
> Human authority record:
> `calibration/src-001/core-design-basis/AUTHORIZED-slice-5-implementation-20260903.md`

This is the producer-authored pre-audit implementation reconciliation for
audited head `46a46359ab63084592d36fc8c430e63afa603992` (tree
`499eff7006e8b0d653020df8e1c86a9ac0d0c5f4`). It is retained as a historical
snapshot; current repair evidence is recorded in
`16-slice-5-implementation-repair-reconciliation.md`.

This document reconciles the implementation branch with the adopted Slice 5
internal-ambiguity/referent lifecycle and adopted OQ-01 human procedural
authority doctrine. It is not an independent audit or authority acceptance
record. A deterministic `PASS` means only the named structural, checker,
fixture, or process proposition. It does not mean replay validation, semantic
validation, agent sanction, acceptance, production readiness, golden status,
or Aleph v1.

## Exact authority and adopted subjects

The implementation authority preserves the exact human declaration:

```text
i authorize slice 5 implementation
```

The authority is pinned to:

- Slice 5 proposal commit
  `ffdd65bb6a6e599b3d506b8c3bd8b9b9900d586c`, tree
  `58f957cfc2ca4f4dd90d7cb2a49b1c8894e9b128`, blob
  `e59f9a0eabd84f71b0b32b3038686cb08fa287d4`, SHA-256
  `df018a37b1a1721be40c445b52d3595083471483ddfd5b306da76da3ad366bc8`;
- OQ-01 proposal commit
  `fd20fd5593963f0ced7646594f5b243d99a06307`, tree
  `0df86da1b4b6c41ea8b305dd6c0d5e44d9a10e24`, blob
  `536a4e447922ae6c606654de403b1fd725e99b1c`, SHA-256
  `73b2cd508d3fe2b7474dd80d49f78b9e55c74a4113557d25bac70880da8f4669`.

The separate adoption records remain unchanged.

## Implemented surface

- Run format `1.5.0-provisional` adds cumulative capability
  `internal-ambiguity-lifecycle`; 1.0–1.4 remain readable under their original
  contracts and reject 1.5-only artifacts.
- T5.1, T5.2, and T5.3 use one exact canonical ledger with deterministic
  parsing, serialization, review-subject digests, closed vocabularies, exact
  expression reopening, same-source search-basis accounting, relation
  eligibility, and the exact state matrix.
- S4 is a durable C1/C2/C3 composite barrier. K2.16 recognizes the legal 1.5
  C1-only state without weakening historical relation checks. K2.17 validates
  retained 1.5 ambiguity and authority state and is explicitly structural
  only.
- Core owns the pinned requirement resolver, operative-scope validation,
  action projection, authority subjects, M/Q sequencing, follow-up planning,
  restriction overlay, exact text blobs, and closure phases.
- The Loa adapter owns mechanical bundle loading, worker dispatch/plumbing,
  journaled persistence, exact-once response application, refusal before
  illegal byte changes, durable halt, and same-pin resume. It does not own
  Slice 5 semantic decisions.
- Dedicated role IDs are `ambiguity-producer`, `ambiguity-reviewer`,
  `material-impact-producer`, and `material-impact-reviewer`. Their bundles
  are sealed read-only and withhold human response and observation bytes.

## MP01–MP08 and S5-A-01 mapping

| Obligation | Implementation evidence | Boundary |
|------------|-------------------------|----------|
| MP01 | `resolvePinnedCoreRequirement` verifies the retained bundle lock, manifest projection, exact Core inventory, bundle/Core identity, file digest, and bytes | no working-tree or mutable-main authority |
| MP02 | exact `core:<path>#<selector>` parser plus unique ATX-heading/outside-fence or durable-token resolver | no trimming, normalization, fuzzy selectors, line selectors, adapter/admin paths, or traversal |
| MP03 | action projection consumes typed operative rows only; prose-only mutation tests preserve behavior | prose remains retained and non-operative |
| MP04 | Core derives presentation from the authority subject; structural comparison rejects hidden scope/actions/consequences/locators/authority | no recommendation or semantic interpretation |
| MP05 | generic mechanical journal/recovery plus single-headed M/Q planners and ten crash positions | recovery never chooses an action or fabricates a response |
| MP06 | restriction overlay derives exact operative tuples from valid retained authority state | no disposition, evidence, relation, S5, or S6 conflation |
| MP07 | T5.3 construction and K2.17 require `selected_candidate_ref = none` | human procedure cannot select a candidate |
| MP08 | manifests preserve manual-only sanction, exact status distinctions, and Hermes planned/full-mode-false | no agent sanction or mode upgrade |
| S5-A-01 | phase-aware K2.16 accepts complete valid relations at 1.5 C1 while rejecting missing/invalid relations and all post-C1 mutation | 1.0–1.4 behavior and relation semantics remain unchanged |

## Deterministic and process evidence

The focused Core contract suite covers canonical ordering, candidate grammar,
search/review subjects, operative scope, action projection, material and
authority subjects, exact request/response bytes, M/Q sequencing, closure
phases, all eight state-matrix branches, and immutable pinned-Core resolution.

The deterministic mutation battery covers ambiguity identity/expression
errors; source/currentness/search errors; candidate and relation eligibility;
review staleness and duplicate-digest inflation; C1/C2 activation; all illegal
matrix rows; material-impact and operative-scope corruption; pinned
requirement-ref failures; action projection and display corruption; T5.3,
request/response, M/Q, stale-response, and positive-legacy-format failures.

The temporal Loa process suite exercises legal pre-C1 relation writes,
immediate post-C1 read-only refusal, all C1/C2/C3 windows, exact-once authority
application, durable request halt, same-pin resume, nonterminal Q+1, material
M+1/Q+1, presentation replacement, worker withholding, and all ten required
crash positions. These are process tests of the implementation path, not
semantic validation.

## Preserved findings and boundaries

- F-03 remains historically unvalidated end-to-end until a fresh independent
  audit decides whether the live-path evidence is sufficient. This
  implementation does not close it itself.
- F-04 remains unresolved unless a separate portability proof establishes it.
- F-05 remains tied to F-03 and is not upgraded here.
- S5-A-03 remains historically LATER. Duplicate review-subject digest
  inflation is rejected because the adopted Slice 5 contract requires it;
  this does not independently close S5-A-03.
- No Aleph Corpus Graph, forecasting, probability, guided-auto, stage-review,
  general rollback/correction, cross-run reuse, projection redesign, Slice 6,
  replay-validation claim, semantic-validation claim, sanction, acceptance,
  production readiness, golden status, or v1 is implemented or claimed.

## Required successor

The implementation branch requires a fresh independent implementation audit.
It must not be merged on the strength of this reconciliation.
