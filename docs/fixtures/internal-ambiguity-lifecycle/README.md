# Internal Ambiguity Lifecycle Fixture

```aleph-fixture
kind: run
src_ids: SRC-0401
```

This synthetic `1.5.0-provisional` run cumulatively exercises exact evidence,
source walking, typed unit lineage, typed relations, and the
`aleph-internal-ambiguity/v1` contract. Its five ambiguity units cover local
resolution, local resolution with a reviewed relation dependency, both typed
null states, multiple candidates, and the legal retained state combinations.

`qualifier-context` and every other relation remain non-evidentiary. The
relation rows do not alter the empty `CC x SRC` evidence-role ledger or its
zero accounting. The retained verifier records are fixture-simulated exact
subject bindings. `STRUCTURAL PASS` means only that the retained files satisfy
the applicable deterministic kernels. It is not `SEMANTIC VALIDATION`: these
artifacts do not prove ambiguity detection, candidate correctness, search
cognitive adequacy, affected-relation completeness, material-impact
correctness, replay, sanction, acceptance, production readiness, golden
status, or v1.

## Adopted F5 case map

| case | retained fixture branch |
|------|-------------------------|
| F5-01 | AMB-1501 single local PKT candidate, resolved-local, empty affected set |
| F5-02 | AMB-1502 single local PKT candidate, resolved-local, reviewed affected REL |
| F5-03 | AMB-1503 full-same-source `null-no-candidate`, unresolved with no carry |
| F5-04 | AMB-1504 full-same-source `null-cannot-determine`, unresolved with explicit affected REL |
| F5-05 | AMB-1505 multiple same-source candidates retained without ranking or score |
| F5-06 | exact UTF-8 expression interval/hash/base64 reopening |
| F5-07 | local search over ordered existing WLK intervals |
| F5-08 | complete full-source walk/completion binding |
| F5-09 | exact upheld ambiguity-review subject binding |
| F5-10 | explicit eligible S4-C1 relation consumption without graph propagation |
| F5-11 | all eight resolution/carry/affected-set combinations in the focused Core suite |
| F5-12 | stale/wrong/duplicate review and relation/state failures in the mutation battery |
| F5-13 | positive 1.5 activation and rejection of the same artifact under legacy format |

The fixture-simulated retained run includes a Class B no-request path and two
Class C paths with exact fixture-simulated human responses: one
`carry-unresolved` and one `restrict-downstream-use`. All six actions,
nonterminal Q+1, material M+1/Q+1, presentation replacement, suspensive
resume, exact optional comment/observation bytes, withholding, and the ten
crash positions are exercised by generated temporary runs in
`test-internal-ambiguity-contracts.ts`,
`test-internal-ambiguity-mutations.ts`, and
`adapters/loa/tests/test-slice5-process.ts`. Those tests are structural and
procedural evidence only; semantic review cases remain sealed subjects for a
later independent audit.
