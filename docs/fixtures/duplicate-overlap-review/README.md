# Duplicate versus overlap review fixtures

These are synthetic Slice 8 challenge declarations, not semantic ground truth
or reference answers. The discovered `positive` and `unresolved` children use
cumulative run format `1.8.0-provisional` and retain their own immutable prompt
and bundle pins. Both include S4 C1/C2/C3 accounting. No retained predecessor
fixture is migrated.

`cases.json` records D8-F01 through D8-F28. The executable family suite also
constructs competing coherent outcomes, deliberately wrong semantic
declarations, separate equal-text occurrences, direct groups, source-bound
ambiguity/material context, conditional round two, empty inventories and
historical failures. Deterministic success carries no semantic warrant or
exhaustive recall claim.

Run `npm run test:duplicate-review` for actual source/generated-runtime report
comparison across contracts, all fixture families, structural mutations and
the internal process suite. `DUPLICATE_NODE20` may name the supported Node 20
binary for the generated-runtime installed-path and recovery checks.

The process suite uses fixture callbacks through sealed assembly, dispatch,
accepted returns and the canonical writer. Static context declarations and
synthetic manual actor/pass records remain explicitly labeled. D8-P01 is
`NOT RUN — genuine native invocation requires separate authority`. No human
review or genuine model invocation is represented by these fixtures.

No fixture closes F-03/F-04/F-05 or confers semantic validation, agent sanction,
acceptance, production readiness, golden status or Aleph v1. Manual mode
remains the only sanctioned execution mode. Slice 9 and replay are outside
this fixture package.
