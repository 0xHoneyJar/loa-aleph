# Synthetic duplicate review

```aleph-fixture
kind: run
```

These are synthetic challenge declarations, not semantic ground truth. Actor/pass names represent static test data; no human or model review was executed. Passing structure carries no semantic warrant, exhaustive recall, or F-03 closure.
