# Typed Relations Fixture

```aleph-fixture
kind: run
src_ids: SRC-401
```

This synthetic `1.4.0-provisional` run cumulatively exercises exact evidence,
source walking, typed unit lineage, and the `aleph-relations/v1` contract.
Its relation ledger covers all eight relation types, each canonical typed-null
scope, an exact `md-lines` source locus, a legal mixed formal/configuration
cycle, and explicit targeting of current successor `CC-0421` rather than
historical predecessor `CC-0420`.

`qualifier-context` and every other relation remain non-evidentiary. The
relation rows do not alter the empty `CC x SRC` evidence-role ledger or its
zero accounting. The retained verifier records are fixture-simulated exact
subject bindings, while `verification/semantic-review-cases.md` exercises
semantic challenges outside K2.16. These artifacts are structural
implementation evidence only and do not prove semantic judgment, replay,
validation, sanction, acceptance, production readiness, golden status, or v1.
